import { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'com.vortex.aicinema',
  appName: 'Vortex AI Cinema',
  webDir: 'dist',
  bundledWebRuntime: false
};
export default config;
